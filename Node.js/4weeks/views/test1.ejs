<html>
    <head>
        <title>EJS</title>
    </head>
    <body>
        <% for (var i = 0; i < 5; i++ ) {%>
            <% if ( i == 3 ) { %>
                <h3>hi</h3>
            <% } else { %>
                <h1>hi</h1>
            <% } %>    
        <% } %>

        <% var a = 'hello'; %>
        <p><%=a%></p>

        <%- include('test2.ejs') %>

        <!--이걸 자바스크립트로 쓴 경우
            for ( var i == 0; i <5; i++) {
            if ( i == 3 ) {
                //h3 태그
            } else {
                //h1 태그
            }
        }-->
    </body>
</html>
